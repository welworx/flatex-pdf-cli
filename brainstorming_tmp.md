⏺ User answered Claude's questions:
· What should the flatex-pdf-cli tool primarily do with the PDFs? → Extract structured data

Good, so the core purpose is structured-data extraction. Next question: which document type(s) should it handle first?

⏺ User answered Claude's questions:
· Which flatex document type should the CLI handle first? → I will point the cli at a pdf file or folder of pdf files and it should parse all of them. The cli based tool will be wired via Skill to my coding agents and the task is to parse the documents efficiently into json so that the can be processed afterwards more efficiently. so all the types, but we start with trade confirmations.

⏺ User answered Claude's questions:
· What language/runtime should the CLI be built in? → dont want that enduser has to deal with missing dependencies or so. therefore i was thinking of go.
